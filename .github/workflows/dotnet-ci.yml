name: .NET CI

on:
  push:
    branches: [ master ]
    paths-ignore:
      - "AutoTest.Web/ClientApp/**"
      - "**/*.md"
  pull_request:
    branches: [ master ]
    paths-ignore:
      - "AutoTest.Web/ClientApp/**"
      - "**/*.md"

concurrency:
  group: dotnet-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: 10.0.x
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore
    - name: Test Unit
      run: dotnet test ./Test/AutoTest.Unit.Test --configuration Release --no-build --no-restore --verbosity normal --logger "console;verbosity=detailed"
    - name: Test Integration
      run: dotnet test ./Test/AutoTest.Integration.Test --configuration Release --no-build --no-restore --verbosity normal --logger "console;verbosity=detailed"
