// import { createStore } from "redux";

// import { eventReducer } from "../../store/event/reducers";
// import { SyncTestRuns } from "../../store/event/actions";
// import thunk from "redux-thunk";

// test("should dispatch secondTestActions ", () => {
//     const realStore = createStore(eventReducer, [ thunk ]);
//     realStore.dispatch(SyncTestRuns(1, 2, ""));
//     // expect(realStore.getActions()).toContainEqual(secondAction());
// });
